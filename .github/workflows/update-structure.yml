name: Update Repo Structure

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  update-structure:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # <-- IMPORTANT: gives GITHUB_TOKEN write access
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Generate & update README
        run: python update_readme.py

      - name: Commit changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Auto-update repo structure" || echo "No changes to commit"
          git push origin HEAD:main
